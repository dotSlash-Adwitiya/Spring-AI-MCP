server:
  port: 8081

spring:
  application:
    name: mcp-server
  ai:
    mcp:
      server:
        name: demo-mcp-server
        version: 1.0.0
        type: SYNC          # Sync mode — consistent with our choice
        protocol: SSE       # SSE transport over HTTP